// import { createClient } from 'redis';
//
// export const redisPublisher = createClient({ url: 'redis://redis:6379' });
// export const redisSubscriber = createClient({ url: 'redis://redis:6379' });
//
// redisPublisher.connect().catch(console.error);
// redisSubscriber.connect().catch(console.error);
//
// redisSubscriber.on('error', (err) => console.error('Redis Subscriber Error:', err));
// redisPublisher.on('error', (err) => console.error('Redis Publisher Error:', err));
